# Kisan-Related Government Scheme APIs (data.gov.in)

## Scope
This list targets data.gov.in API resources whose titles include 'kisan' and indicate a government scheme (e.g., scheme/yojana/PM-KISAN).

## Sources
- data.gov.in API directory (API resources)
- data.gov.in backend listing endpoint: https://www.data.gov.in/backend/dataapi/v1/lists

## Access Requirements
Data access requires a data.gov.in API key. Use the standard resource endpoint:
- https://api.data.gov.in/resource/{resource_id}?api-key=YOUR_API_KEY&format=json

## Status
Metadata fetch attempted on 2026-02-10.
The metadata endpoint was not reachable from this environment (connection reset).
No dataset metadata could be retrieved, so counts below are zero pending a successful sync.

## Counts
- Title contains 'kisan' (deduplicated by resource id): 0
- Title contains 'kisan' + scheme-related keyword (deduplicated): 0

## Resource List
Fields: `Title` | `Resource ID` | `Organization` | `Updated (UTC)` | `API Endpoint Template`
- (empty; metadata sync failed)
